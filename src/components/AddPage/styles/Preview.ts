import {styled} from "styled-components";

const BookWrapper = styled.div`
    border: white solid 0.2vw;
    border-radius: 0.8vw;
    position: relative;
    top: 6vw;
    display: flex;
    flex-direction: row;
`
const Title = styled.div`
    position: relative;
    top: 3vw;
`

export {BookWrapper, Title};